use anyhow::Result;

pub async fn run() -> Result<()> {
    // TODO Phase 2:
    // 1. Check key exists and can be unlocked
    // 2. Check SQLite DB accessible
    // 3. Test connectivity to each configured relay
    // 4. Report status
    println!("mycel doctor — not yet implemented");
    Ok(())
}
