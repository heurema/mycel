use anyhow::Result;

pub async fn run() -> Result<()> {
    // TODO Phase 0:
    // 1. Generate secp256k1 keypair via nostr-sdk
    // 2. Encrypt and store private key (keychain or passphrase file)
    // 3. Create SQLite database with schema
    // 4. Write default config (relay list)
    // 5. Test relay connectivity
    // 6. Print own address (npub)
    println!("mycel init — not yet implemented");
    Ok(())
}
